# File-Organizer-Automation
Python automation script that organizes files by extension. Uses the OS module, user input, exception handling, and log generation. Automatically creates folders and moves files into the appropriate directory for easy file management.
